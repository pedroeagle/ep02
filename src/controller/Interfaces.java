package controller;

import model.User;
import view.Login;
import view.Main;
import view.TypesList;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;


public class Interfaces extends JFrame{
    public Interfaces() {

        TypesList typesList = null;

    }
    /*main.typesList = new JList(types.getNameTypes());
        main.typesList.setListData(types.getNameTypes());
        main.typesScroll = new JScrollPane(main.typesList);


        mainTab.getContentPane().add(main.typesScroll);
        main.typesScroll.setPreferredSize(new Dimension(250, 80));*/
}
